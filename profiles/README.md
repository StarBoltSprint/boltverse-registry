# profiles/

One file per player: `profiles/<userId>.json`.

Auto-save from Welcome register (chat start when `sub` known — GitHub Action `pack-welcome` / `scripts/pack-welcome.mjs`; existence + lastSeen; **not** `*.grok.me`) · Phase 2 `boltverse-pack` boot / heartbeat (`playTimeSec`; **not** any Live) · Hang · Keep · seal · remix · publish.

Index mirror: root `registry.json`.
