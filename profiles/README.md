# profiles/

One file per player: `profiles/<userId>.json`.

Auto-save from Welcome register (chat start when `sub` known — GitHub Action `pack-welcome` / `scripts/pack-welcome.mjs`; existence + lastSeen; **not** `*.grok.me`) · Play boot / heartbeat later (`playTimeSec`, optional) · Hang · Keep · seal · remix · publish.

Index mirror: root `registry.json`.
